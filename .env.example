# Server-only (do NOT commit real secrets)
DISCOURSE_URL=https://nextelite.discourse.group
# Optional server-only API key (only if you need private endpoints)
# DISCOURSE_API_KEY=your_discourse_api_key
# DISCOURSE_API_USERNAME=system

# Public (safe for use in the browser) - used for building links only
NEXT_PUBLIC_DISCOURSE_URL=https://nextelite.discourse.group
